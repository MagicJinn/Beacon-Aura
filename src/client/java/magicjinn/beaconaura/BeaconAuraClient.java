package magicjinn.beaconaura;

import net.fabricmc.api.ClientModInitializer;

public class BeaconAuraClient implements ClientModInitializer {
    @Override
    public void onInitializeClient() {
        // No-op (no client-only setup)
    }
}